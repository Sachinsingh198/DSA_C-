// Rotate the given array 'a by k steps , where k is the non-negative.
//Note: k can be freater than n as well where n is the size of array 'a'.
#include <iostream>
using namespace std;
int main(){
    return 0;
}